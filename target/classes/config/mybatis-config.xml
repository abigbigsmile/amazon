<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

<!--
    <properties resource="db.properties"></properties>
    &lt;!&ndash;
        别名设置，可以设置单个别名，同时也可以通过扫描包名的方式批量设置别名，别名为类名（开头大小写都可以）
     &ndash;&gt;
    <typeAliases>
        &lt;!&ndash;  单个别名设置 &ndash;&gt;
        &lt;!&ndash;<typeAlias type="com.smile.mybatis.mapper.UserMapper" alias="userMapper"></typeAlias>&ndash;&gt;
        &lt;!&ndash;  扫描包 &ndash;&gt;
        <package name="com.smile.springmvc.model"></package>
        <package name="com.smile.springmvc.mapper"></package>
    </typeAliases>

    <environments default="development">
        <environment id="development">
            &lt;!&ndash; 使用jdbc事务管理，由mybatis控制 &ndash;&gt;
            <transactionManager type="JDBC"/>
            <dataSource type="POOLED">
                <property name="driver" value="${jdbc.driverClassName}"/>
                <property name="url" value="${jdbc.url}"/>
                <property name="username" value="${jdbc.username}"/>
                <property name="password" value="${jdbc.password}"/>
            </dataSource>
        </environment>
    </environments>

    <mappers>
        <mapper resource="mapper/userMapper.xml"/>
    </mappers>
    -->

    <settings>
        <setting name="lazyLoadingEnabled" value="true"/>
        <setting name="aggressiveLazyLoading" value="false"/>
    </settings>


</configuration>
